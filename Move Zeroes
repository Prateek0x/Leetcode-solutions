class Solution {
public:
    void moveZeroes(vector<int>& arry) {
        int index=0;
        for(int i=0;i<arry.size();i++){
            if(arry[i]!=0){
                swap(arry[i],arry[index]);
                index++;
            }
        }
        
    }
};
