class Solution {
    public:
  void reverseString(vector<char>& s) {
     
     stack<int> stk;

     for(int i = 0; i<s.size();i++) {
        stk.push(s[i]);
     }

     for(int j=0; j<s.size();j++) {
        s[j] = stk.top();
        stk.pop();
     }
  }
};
