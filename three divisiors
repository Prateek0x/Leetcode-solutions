class Solution {
public:
    bool isThree(int n) {
        int c=0;
        for(int k=1;k<=n;k++){
            if(n%k == 0) c++;
        }
        if(c == 3) return true;
        else return false;
    }
};
