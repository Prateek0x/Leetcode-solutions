class Solution {
public:
    int sumBase(int n, int k) {
        int count =0;

        while(n>=k){
            cout<<" n : "<<n<<endl;
            count += n % k;
            n /= k;
        }

        return count  + n;
    }
};
